[package]
name = "livi-video-fanout"
version = "0.1.0"
edition.workspace = true

# Per-receiver frame gating and GOP cache, driven by gst_video.cc through
# cp_video_fanout.h. The caller owns the planes and does the pushing.
[lib]
crate-type = ["rlib"]

[dependencies]
livi-video-nal = { path = "../nal" }
