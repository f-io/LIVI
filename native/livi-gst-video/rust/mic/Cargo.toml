[package]
name = "livi-audio-uplink"
version = "0.1.0"
edition.workspace = true

# The CarPlay microphone stream: capture, encode, seal, send.
[lib]
crate-type = ["rlib"]

[dependencies]
livi-crypto-node = { path = "../../../livi-crypto/rust", default-features = false }
